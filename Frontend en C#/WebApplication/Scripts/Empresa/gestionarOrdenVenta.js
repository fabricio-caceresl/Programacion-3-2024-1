// Función para mostrar el modal de Cliente
function showModalFormCliente() {
    var modalForm = new bootstrap.Modal(document.getElementById('form-modal-cliente'));
    modalForm.toggle();
}

// Función para mostrar el modal de Producto
function showModalFormProducto() {
    var modalForm = new bootstrap.Modal(document.getElementById('form-modal-producto'));
    modalForm.toggle();
}